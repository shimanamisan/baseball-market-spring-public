# 2026-06-13 リプレース進捗分析

旧 `baseball-market`（PHP / DDD レイヤード）→ `baseball-market-spring`（Spring Boot）フルリプレースの進捗状況と今後の作業の分析記録。

## 全体像

リプレースはフェーズ制で進んでおり、**フェーズ 7 まで完了**（各フェーズの詳細は `_docs/changelog/` を参照）。
旧 PHP 側の 6 つの Bounded Context（User / Product / Message / Like / MyPage / Shared）のうち、**4 つの移植が完了**している。

| フェーズ | 内容 | 状態 | 完了日 |
| --- | --- | --- | --- |
| 1〜2 | 基盤整備・Flyway `V1__init.sql` | ✅ 完了 | 2026-05-11 |
| 3 | shared コンテキスト | ✅ 完了 | 2026-05-11 |
| 4〜5 | user コンテキスト（signup・メール認証・login・パスワード編集/リマインド・プロフィール編集・退会の全 8 機能） | ✅ 完了 | 2026-05-11 |
| 6 | product コンテキスト（一覧・詳細・出品・売買履歴） | ✅ 完了 | 2026-05-11 |
| 7 | message コンテキスト + 購入フロー開通（`PurchaseService` による product↔message 跨ぎのトランザクション） | ✅ 完了 | 2026-06-09 |
| 8 | **mypage コンテキスト** | ⬜ 未着手 | — |
| — | **like コンテキスト（Ajax いいね）** | ⬜ 未着手 | — |

フェーズ 7 以降も改善コミットが継続している:

- 購入処理への悲観ロック導入（PR #16）
- メッセージ送信バリデーションエラー時の入力本文保持（PR #25、2026-06-13 時点の最新）

いずれも `develop` にマージ済み。working tree はクリーンで、未マージの PR・オープンな issue はない。

### 現在動く主動線

「ユーザー登録 → メール認証 → ログイン → 出品 → 一覧 → 詳細 → 購入 → 掲示板でメッセージ連絡」が一通り回る状態。

### ドキュメントの乖離

リポジトリ直下の `CLAUDE.md` は「機能リプレースは未着手」「Flyway マイグレーション未作成」のまま古い記述になっている（実際はフェーズ 7 まで完了）。**要更新**。

## 今後取り掛かるべき内容

### 1. フェーズ 8: mypage コンテキスト（次フェーズとして計画済み）

フェーズ 7 changelog の「次フェーズ」に明記されている作業:

- 出品商品 / お気に入り / **参加中の掲示板一覧**の集約表示（旧 `MyPage/Application/MyPageService` 相当）
- 暫定 `HomeController` の `/mypage` を本実装に置換
- フェーズ 6 で Native Query 妥協した売買履歴の再評価（Board Entity が揃ったため JPQL 化 or Orchestrator 化を検討）

現状、掲示板へは「購入直後リダイレクト」か URL 直打ちでしか到達できないため、本フェーズで導線が完成する。

### 2. like コンテキスト（最後の未移植 Context）

旧 `Like/Presentation/LikeController.php`（Ajax いいね）の移植。他 context から独立しているため後回しで問題ない設計だが、mypage の「お気に入り一覧」が likes テーブルを参照するため、**フェーズ 8 と like の実装順序を着手前に決める必要がある**（like を先行させる選択肢もある）。

### 3. 積み残しの残課題（フェーズ 7 changelog 記載）

| 項目 | 状況 |
| --- | --- |
| `db` ホスト名の別名衝突（環境問題） | 素の `./gradlew test` で `contextLoads` が失敗する。恒久対応の候補は ①テスト用プロファイル（`application-test.properties`）の導入、②環境変数（`SPRING_DATASOURCE_URL`）による接続先の一意化（フェーズ 7 検証で全テスト緑を確認済みの手法）、③ネットワーク分離（いずれも未実施）。`application.properties` を `baseball-market-spring-db` に直接書き換える方法はローカル／CI 間のポータビリティを下げるため、プロファイル・環境変数による動的制御を優先する |
| `messages.bord_id` のタイポ列 | 当面保持。リネームは将来の独立マイグレーション（V2 以降）で |
| 掲示板表示の OSIV 依存 | OSIV を切る判断をする場合は表示用 DTO 化が必要 |
| メッセージのリアルタイム更新 | 旧 PHP 同様ページリロード方式。WebSocket 等は対象外 |

### 4. リリース運用

git-flow ルール（`.claude/rules/git-flow.md`）では「`develop` → `main` マージ + `vX.Y.Z` タグ付与」が正式手順だが、`main` は `develop` と同期済みなのに**バージョンタグが一つも打たれていない**。フェーズ 7 完了は節目のため、タグ付与（例: `v0.3.0`）を検討する余地がある。

## 推奨の次アクション

1. mypage と like の実装順序を決定する（お気に入り一覧の扱いが分岐点）
2. フェーズ 8（mypage コンテキスト）へ着手
3. 併行して `CLAUDE.md` の現状記述を更新し、`db` 別名衝突の恒久対応・バージョンタグ付与を別件として処理
